import { ProjectConfig } from "../types/settingT";

const setting: ProjectConfig = {
  //是否显示SideBar侧边栏
  showSideBar: true,
  header:{
    background:'rgb(236, 65, 65)'
  },
  sideWindow:{
    backgroundColor:'#fff'
  }
};

export default setting;
